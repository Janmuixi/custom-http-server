### Custom HTTP Server

This is just a sandbox for investigation on how http server on node works. The final purpose would be to build a fast framework to add create an httpserver, add new routers and start working.